@model WebApplication1.ViewModels.DevisViewModels
@{
    Layout = "~/Views/Shared/_LayoutPF.cshtml";
    ViewBag.Title = "Accueil Dashboard";

}

<head>
    <link rel="stylesheet" href="~/css/connecte.css">
</head>
<div class="container-fluid titre">
    <h1>Bienvenue dans votre espace</h1>
</div>

<div class="container">
    <h2 class="titreEntete">Mes devis existant</h2>
    <div class="row rowCards">
        @foreach (var item in Model.dvm.MainDevis)
        {
            <div class="col-md-3 col-sm-6">
                <div class="card cardConnecte">

                    <img src="~/images/Evenements/DEVIS.png"
                         class="card-img-top imgDevis"
                         alt="..." />
                    <div class="filtreVert">
                        <div>@Html.ActionLink("Détails", "RecapitulatifDevis", "Devis", new { id = item.Id }, new { @class = " btnReveal card-link", @style = "text-align:center; top: 25%" })</div>
                    </div>

                    <div class="card-body">
                        <h5 class="card-title">Devis N° : @string.Concat("D478",item.Id)</h5>
                        <div class="card-text">
                            <div><strong>Date :</strong> @item.DateEmissionDevis.ToShortDateString()</div>
                            <div><strong>Nom de l'événement :</strong> @item.EventDevis.ThemeEvent</div>
                            <div><strong>Type d'événement :</strong> @item.EventDevis.TypeEvent</div>
                            <div><strong>Etat du devis :</strong> @item.AvancementDevis</div>
                            @if (item.AvancementDevis != WebApplication1.Models.StatutDevis.Demande)
                            {
                                <div><strong>Coût:</strong> @string.Concat(item.CoutEstime," €")</div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<div class="container">
    <h2>Créer un nouveau devis</h2>
    <div class="row rowCards">
        <div class="col-md-3 col-sm-6">
            <div class="card cardConnecte">
                <img src="~/images/Evenements/plus_devis.png"
                     class="card-img-top imgDevis"
                     alt="..." />
                <div class="filtreVert">
                    <div >@Html.ActionLink("Nouveau", "FormulaireDevis2Connecte", "Devis", new { id = ViewBag.id }, new { @class = " btnReveal card-link", @style="text-align:center; top: 50%" })</div>
                   
                </div>
            </div>
        </div>
    </div>
</div>

    <div class="container">
        <h2 class="titreEntete">Mes événements</h2>
        <div class="row rowCards">
            @foreach (var item in Model.dvm.MainDevis)
            {
                if(item.EventDevis.AvancementEvent == WebApplication1.Models.AvancementEvenement.AVenir) { 
                <div class="col-md-3 col-sm-6">
                    
                    <div class="card cardConnecte">
                        @if (item.EventDevis.TypeEvent == WebApplication1.Models.TypeEvenement.Seminaire)
                        {
                            <img src="~/images/Evenements/Seminaire2.jpeg"
                                 class="card-img-top"
                                 alt="..." />
                        }
                        else if (item.EventDevis.TypeEvent == WebApplication1.Models.TypeEvenement.TeamBuilding)
                        {
                            <img src="~/images/Evenements/TeamBuilding.jpeg"
                                 class="card-img-top"
                                 alt="..." />
                        }
                        else if (item.EventDevis.TypeEvent == WebApplication1.Models.TypeEvenement.RoadShow)
                        {
                            <img src="~/images/Evenements/Seminaire.jpeg"
                                 class="card-img-top"
                                 alt="..." />
                        }
                        else if (item.EventDevis.TypeEvent == WebApplication1.Models.TypeEvenement.FamilyDay)
                        {
                            <img src="~/images/Evenements/family-day.jpg"
                                 class="card-img-top"
                                 alt="..." />
                        }
                        else if (item.EventDevis.TypeEvent == WebApplication1.Models.TypeEvenement.LancementProduit)
                        {
                            <img src="~/images/Evenements/new_product.jpg"
                                 class="card-img-top"
                                 alt="..." />
                        }
                        <div class="filtreVert">
                            <div>@Html.ActionLink("Détails", "RecapitulatifEvent", "Devis", new { id = item.Id }, new { @class = " btnReveal card-link", @style = "text-align:center; top: 25%" })</div>
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">@item.EventDevis.TypeEvent</h5>
                            <div class="card-text">
                                <div><strong>Date :</strong> @item.EventDevis.DateEvent.ToShortDateString()</div>
                                <div><strong>Nom de l'évenement :</strong> @item.EventDevis.ThemeEvent </div>
                                @*<div><strong>Type d'événement :</strong> @item.EventDevis.TypeEvent </div>*@
                                <div><strong>Etat de l'événement :</strong> @item.EventDevis.AvancementEvent </div>
                            </div>
                        </div>
                    </div>
                </div>
                }
            }
        </div>
       
    </div>
    @*<div class="container">
        <h2 class="titreEntete">Mes factures</h2>
        <div class="row rowCards">
            <div class="col-md-3 col-sm-6">
                <div class="card cardConnecte">

                    <img src="~/images/Evenements/Facture2.png"
                         class="card-img-top imgDevis"
                         alt="..." />
                    <div class="filtreVert">
                        <a href="#" class=" btnReveal card-link">Détails</a>
                    </div>

                    <div class="card-body">
                        <h5 class="card-title">Facture n°F00523</h5>
                        <p class="card-text">
                            Date</br>
                            Nom de l'évenement</br>
                            Statut
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>*@
